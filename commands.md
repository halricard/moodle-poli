docker compose exec -T moodle sh -lc 'php /var/www/html/admin/cli/purge_caches.php >/dev/null 2>&1; php /var/www/html/admin/cli/build_theme_css.php --themes=poli 2>&1; echo "EXIT=$?"; f=$(ls -t /var/www/moodledata/localcache/theme/*/poli/css/all_*.css | head -1); echo "rev=$(basename $(dirname $(dirname $(dirname $f))))"; echo "=== final override present (last occurrence) ==="; grep -o "body.editing .courseindex .courseindex-link{[^}]*}" "$f" | tail -1'